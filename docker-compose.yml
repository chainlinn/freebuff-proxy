services:
  freebuff-proxy:
    image: santu24/freebuff-proxy:dev
    pull_policy: always
    ports:
      - '9099:8080'
    volumes:
      - ./data:/app/data
    container_name: freebuff-proxy
    restart: unless-stopped
